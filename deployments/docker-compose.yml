version: "3.1"

services:

  payments.test:
    build:
      context: ../
      dockerfile: ./build/docker/Dockerfile
    ports:
      - 8080:8080
    environment:
      DB_USER: admin
      DB_PASSWORD: changeme
      DB_NAME: payments
      DB_HOST: postgres.test
      DB_PORT: 5432
      PORT: 8080
    entrypoint: ["/bin/wait-for", "postgres.test:5432", "--", "/bin/payments-api", "run"]
    depends_on:
      - postgres.test

  postgres.test:
    image: postgres:11
    restart: always
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: changeme
      POSTGRES_DB: payments
    volumes:
      - ./sql/tables.sql:/docker-entrypoint-initdb.d/tables.sql:ro
    ports:
      - 5432:5432
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U admin payments"]
      interval: 10s
      timeout: 5s
      retries: 5
